1. 能不能用？
能，但非常不推荐。
理论上你可以把分类标签（0/1 或 one-hot）当成连续值，用 MSE 训练。
2. 为什么分类不用 MSE？（面试高频考点）
梯度消失
对 sigmoid/softmax 输出的概率，当预测很 “自信”（接近 0 或 1）时，MSE 的梯度会变得极小，网络很难学、收敛极慢。
损失函数非凸
MSE + softmax/sigmoid 会形成非凸损失，容易陷入局部最优，训练不稳定。
目标不匹配
MSE：衡量数值距离，适合回归。
分类：要衡量概率分布之间的差异，交叉熵更匹配。
3. 一句话总结（面试直接说）
MSE 可以用于分类，但会带来梯度消失、损失非凸、收敛慢等问题，分类任务标准做法是用交叉熵损失。
